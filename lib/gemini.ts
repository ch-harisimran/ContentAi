// UNUSED — switched from Gemini to OpenRouter (see lib/openrouter.ts).
// The Gemini API key kept getting the associated Google Cloud project
// denied ("contact support") for this account, which isn't something
// fixable from code. Nothing imports this file anymore; it's safe to
// delete along with the `@google/genai` package in package.json whenever
// convenient.
